import { json } from '@sveltejs/kit'; import { AIGateway } from '$server/ai/AIGateway'; import { ScamScoreService } from '$server/services/ScamScoreService';
const cleanJson=(value:string)=>value.replace(/^```(?:json)?\s*/i,'').replace(/\s*```$/,'').trim();
export const POST=async({request})=>{try{const body=await request.json();const target=String(body.target||'').trim().slice(0,500);const description=String(body.description||'').trim().slice(0,3000);if(!target&&!description)return json({error:'Add a target or suspicious activity description.'},{status:400});const heuristics=new ScamScoreService().score(target);const prompt=`You are Poké's evidence-first Web3 scam-risk analyst. Analyze only the supplied user input and heuristic evidence. Never state that a person or project is fraudulent as a fact unless conclusive sourced evidence exists. Missing data must remain unknown. Return only valid JSON with this exact shape: {"riskLevel":"low|medium|high|critical|unknown","confidence":"low|medium|high","scamType":"short classification such as phishing, fake investment, honeypot, impersonation, wallet drainer, rug-pull risk, or unknown","summary":"concise explanation","evidence":["observation with source or user-provided label"],"avoidance":["how a user can recognize and avoid this type of scam before interacting"],"protection":["what an affected user should do now to protect wallet, accounts, or funds"],"missingInformation":["data needed to improve confidence"],"disclaimer":"risk assessment limitation"}. Give 3 to 6 concise items for avoidance and 3 to 6 different, immediate items for protection. Target: ${target||'not supplied'}\nUser description: ${description||'not supplied'}\nHeuristic evidence: ${JSON.stringify(heuristics.evidence)}`;const completion=await new AIGateway().complete([{role:'system',content:'Return strict JSON only. Do not use Markdown.'},{role:'user',content:prompt}]);const content=completion.content;let analysis;try{analysis=JSON.parse(cleanJson(content));}catch{analysis={riskLevel:'unknown',confidence:'low',scamType:'unknown',summary:content,evidence:[],avoidance:['Verify domains through official channels before connecting a wallet.'],protection:['Do not connect a wallet or send funds until the target is independently verified.'],missingInformation:['Structured AI output could not be validated.'],disclaimer:'This is a risk assessment, not a factual accusation.'};}analysis.evidence=Array.isArray(analysis.evidence)?analysis.evidence:[];analysis.avoidance=Array.isArray(analysis.avoidance)?analysis.avoidance:['Verify the target independently before interacting.'];analysis.protection=Array.isArray(analysis.protection)?analysis.protection:['Disconnect the site and review active wallet approvals.'];analysis.missingInformation=Array.isArray(analysis.missingInformation)?analysis.missingInformation:[];return json({analysis,heuristics:heuristics.evidence,scannedAt:new Date().toISOString(),gonka:{requestId:completion.requestId,model:completion.model}});}catch(error){return json({error:error instanceof Error?error.message:'Scan failed'},{status:503});}};
