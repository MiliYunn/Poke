import { json } from '@sveltejs/kit'; import { decodeFunctionResult, encodeFunctionData, formatUnits, getAddress, isAddress, parseAbi } from 'viem'; import { rpc } from '$server/providers/RpcProvider';
const abi=parseAbi(['function balanceOf(address) view returns (uint256)','function name() view returns (string)','function symbol() view returns (string)','function decimals() view returns (uint8)']);
async function callInfo(chainId:number,to:`0x${string}`,functionName:'name'|'symbol'|'decimals'){const data=encodeFunctionData({abi,functionName});const value=await rpc(chainId,'eth_call',[{to,data},'latest']);return decodeFunctionResult({abi,functionName,data:value});}
async function callBalance(chainId:number,to:`0x${string}`,owner:`0x${string}`){const functionName='balanceOf' as const;const data=encodeFunctionData({abi,functionName,args:[owner]});const value=await rpc(chainId,'eth_call',[{to,data},'latest']);return decodeFunctionResult({abi,functionName,data:value});}
export const POST=async({request})=>{try{const {owner,chainId,contracts=[]}=await request.json();if(!isAddress(owner))return json({error:'Enter a valid wallet address.'},{status:400});const list=(contracts as string[]).filter(value=>isAddress(value)).slice(0,10).map(value=>getAddress(value));if(!list.length)return json({error:'Add at least one valid ERC-20 contract address.'},{status:400});const wallet=getAddress(owner);const tokens=await Promise.all(list.map(async contract=>{try{const [name,symbol,decimals,balance]=await Promise.all([callInfo(chainId,contract,'name'),callInfo(chainId,contract,'symbol'),callInfo(chainId,contract,'decimals'),callBalance(chainId,contract,wallet)]);return{contract,name:String(name),symbol:String(symbol),decimals:Number(decimals),rawBalance:String(balance),balance:formatUnits(balance,Number(decimals)),status:'ok'};}catch(error){return{contract,status:'error',error:error instanceof Error?error.message:'Token query failed'};}}));return json({owner:wallet,chainId,tokens,fetchedAt:new Date().toISOString()});}catch(error){return json({error:error instanceof Error?error.message:'Token discovery failed'},{status:502});}};
