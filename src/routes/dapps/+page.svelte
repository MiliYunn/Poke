<script lang="ts">import AppHeader from '$components/AppHeader.svelte';const interfaces=[{name:'EAS Explorer',description:'Inspect public Ethereum attestations.',url:'https://sepolia.easscan.org/'},{name:'Safe Apps',description:'Open the Safe application directory.',url:'https://app.safe.global/apps'}];let selected=interfaces[0];</script>
<svelte:head><title>Dapp Interfaces — Poké</title></svelte:head><AppHeader active="dapps"/>
<main class="route-page"><section class="route-title"><div><span class="eyebrow">PLUGGABLE DAPP SHELL</span><h1>Protocol interfaces</h1><p>Third-party views are registered as replaceable adapters. Poké never injects wallet credentials into embedded pages.</p></div></section><section class="network-section"><div class="network-cards">{#each interfaces as item}<button class:active={selected.name===item.name} onclick={()=>selected=item}><div><strong>{item.name}</strong><small>{item.description}</small></div><b>Open →</b></button>{/each}</div></section><section class="explorer-workbench"><div class="workbench-head"><div><span class="eyebrow">THIRD-PARTY ORIGIN</span><h2>{selected.name}</h2></div><a href={selected.url} target="_blank" rel="noreferrer">Open safely in new tab ↗</a></div><iframe title={selected.name} src={selected.url} sandbox="allow-scripts allow-same-origin allow-forms allow-popups" style="width:100%;height:650px;border:0"></iframe></section></main>
